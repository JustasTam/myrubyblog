<h1><%= @category.name %></h1>

<% if !@posts.empty? %>
	<% @posts.each do |post| %>
		<h3><%= link_to(post.title,post) %></h3>
		<p><%= post.body %></p>
		<hr>
	<% end %>
<% else %>
	<p>There is nothing to show</p>
<% end %>

<p><%= link_to 'Back', categories_path %></p>
